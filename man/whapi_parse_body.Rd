% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/webhook.R
\name{whapi_parse_body}
\alias{whapi_parse_body}
\title{Parse the raw body of a Plumber request}
\usage{
whapi_parse_body(req)
}
\arguments{
\item{req}{A Plumber request object (list-like).}
}
\value{
A list representing the parsed body, or \code{NULL} if no body is found.
}
\description{
Attempts to parse the body of a Plumber \code{req} object.
Supports both \code{req$body} (already parsed) and \code{req$bodyRaw} (raw binary).
If the body looks like JSON (\code{{...}} or \verb{[...]}), it tries to parse it
with \code{jsonlite::fromJSON}. Otherwise, it returns the raw text.
}
\examples{
# Inside a Plumber endpoint:
# parsed <- whapi_parse_body(req)
# if (!is.null(parsed)) str(parsed)
}
